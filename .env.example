# No env vars needed — authentication uses credentials.json (OAuth 2.0 Desktop app)
# Download from Google Cloud Console → APIs & Services → Credentials
# Save as credentials.json in the project root
